node_modules/
dist/
.DS_Store
*.log
.vercel
.claude/

# Secrets — never commit
.env
.env.local
.env.save

# Office / business docs (keep out of repo)
*.docx
*.xlsx
.~lock.*

# Vite stale build artifacts
vite.config.js.timestamp-*.mjs

# Test eval output
tests/eval_results/

# Stray dirs from earlier scaffold
{src/
out post new/

# Generated DB setup bundle (regenerate with: bash scripts/build_setup_bundle.sh)
prod_setup_bundle.sql

# One-time reset+rebuild paste (reset_public_schema.sql + the bundle, concatenated)
prod_reset_and_rebuild.sql
